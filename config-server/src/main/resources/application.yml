server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/leydysoto/LariAlmacen.git
          search-paths: config-data
  application:
    name: config-server
  security:
    user:
      name: root
      password: s3cr3t

#$ git push --set-upstream origin config-server establer la nueva rama



